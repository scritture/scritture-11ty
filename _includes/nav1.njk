     <!-- Navbar Dark -->

<nav
  class="navbar navbar-expand-lg navbar-dark bg-gradient-dark z-index-3 py-3">
  <div class="container">
    <a class="navbar-brand text-white" href="{{ metadata.url }}" rel="tooltip" title="{{ metadata.title }}" data-placement="bottom" target="_blank">
      {{ metadata.title }}
    </a>
    {#- 
    <a href="{{  metadata.url }}" class="btn btn-sm  bg-gradient-primary  btn-round mb-0 ms-auto d-lg-none d-block">Buy Now</a>
#}
  
    <button class="navbar-toggler shadow-none ms-2" type="button" data-bs-toggle="collapse" data-bs-target="#navigation" aria-controls="navigation" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon mt-2">
        <span class="navbar-toggler-bar bar1"></span>
        <span class="navbar-toggler-bar bar2"></span>
        <span class="navbar-toggler-bar bar3"></span>
      </span>
    </button>
    <div class="collapse navbar-collapse w-100 pt-3 pb-2 py-lg-0" id="navigation">
      <ul class="navbar-nav navbar-nav-hover mx-auto">

      {%- for entry in collections.all | eleventyNavigation %}
        <li class="nav-item dropdown dropdown-hover mx-2 {% if entry.url == page.url %} nav-item-active{% endif %}">
          <a href="{{ entry.url | url }}" 
            class="nav-link ps-2 d-flex justify-content-between cursor-pointer align-items-center" 
            role="button">
            {{ entry.title }}
          </a>
        </li>
      {%- endfor %}

        <li class="nav-item dropdown dropdown-hover mx-2">
          <a href="/demo" class="nav-link ps-2 d-flex justify-content-between cursor-pointer align-items-center" role="button">
            demo
             {#-
            <img src="/assets/img/down-arrow-white.svg" alt="down-arrow" class="arrow ms-1"> #}
          </a>
        </li>

      </ul>
 {#- bottone  a dx  
      <ul class="navbar-nav d-lg-block d-none">
        <li class="nav-item">
          <a href="#" 
          class="btn btn-sm  bg-gradient-primary  btn-round mb-0 me-1" role="button">go to</a>
        </li>
      </ul>
#}
    </div>
  </div>
</nav>
<!-- End Navbar -->

   {#-    
     <h1 class="home"><a href="{{ '/' | url }}">{{ metadata.title }}</a></h1>
 #}
      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ 


      <ul class="nav">
      {%- for entry in collections.all | eleventyNavigation %}
        <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title }}</a></li>
      {%- endfor %}
      </ul>

#}
